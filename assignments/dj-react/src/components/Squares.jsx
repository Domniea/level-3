import React from 'react'

export default function Squares(prop) {
    
    return (
        <div className='squares' style={{backgroundColor: `${prop.color}`}} >
            <h1></h1>
        </div>
    )
}